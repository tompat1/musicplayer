# musicplayer

Winamp-styled retro player for all my music productions.

## Run locally

```sh
npm install
npm run dev
```

## Add music

Drop audio files into `public/assets/audio`, then run:

```sh
npm run build
```

The app regenerates `src/audioData.json` before `dev` and `build`.
